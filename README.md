# VAMM

## Run react app
Run the following commands to view the live page:

1. `cd client`

2. `npm install`

3. `npm start`

4. 'cd server'

5. 'npm install'

6. ' npm install nodemon'

7. 'nodemon index.js'
